# MongoDB-Verbindung
MONGO_URI=mongodb://localhost:27017/deinedatenbank

# Server-Port
PORT=3000

# JWT für Authentifizierung
JWT_SECRET=deinSuperGeheimesToken

# Sentry-DSN für Monitoring (nur wenn aktiviert)
SENTRY_DSN=https://user@sentry.io/123456